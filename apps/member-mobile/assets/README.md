# VINYRD assets

`vinyrd-logo.png` is copied unchanged from
`frontend/member-app/assets/vinyrd-logo.png`. It supplies the existing circular
crest for native headers and the app icon. The Expo template license does not
change existing branding ownership or terms.

The original web assets are unchanged. The legacy `vinyrd-crest.webp` was not used
because Metro rejected its image data; the existing PNG bundles successfully.
